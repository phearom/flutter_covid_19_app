import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class StyleHelper {
  static final mainColor = Color(0xff222222);

  static final homeTitle = TextStyle(
    fontSize: 20,
    color: Colors.white,
  );
  static final text1Title = TextStyle(
    fontSize: 15,
    color: Colors.white,
  );

  static final Color textColor = Colors.white;
  static final Color greyColor=Colors.grey[600];
  static final drawerTitle = TextStyle(
    color: Colors.white,
    fontSize: 18,
  );
}
